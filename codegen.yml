overwrite: true
schema: "https://api.spacex.land/graphql/"
documents: "src/**/*.graphq"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
  ./graphql.schema.json:
    plugins:
      - "introspection"
